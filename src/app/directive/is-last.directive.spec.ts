import { IsLastDirective } from './is-last.directive';

describe('IsLastDirective', () => {
  it('should create an instance', () => {
    const directive = new IsLastDirective();
    expect(directive).toBeTruthy();
  });
});
